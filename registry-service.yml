server:
  port: 8881
eureka:
  instance:
    hostname: localhost
  client:
    registryWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/